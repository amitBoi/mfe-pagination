export const TOTAL_ITEMS = 5000;
export const INITIAL_PAGE_NO = 1;
export const ITEMS_PER_PAGE = 1;

export const ITEM_HEIGHT = 30;
export const ITEM_LIMIT = 20;
export const HEIGHT = 400;

export const ROUTES = {
  pagination: '/pagination',
  advancedPagination: '/advanced-pagination',
  infiniteScroll: '/infinite-scroll',
  todosWithAPI: '/todos-with-api',
  todosWithData: '/todos-with-data',
};

export const URLS = {
  TODOS: '/todos',
};
